# 主函数工作流程

- 创建池资源
- 忽略`SIGPIPE`信号
- 创建socket、命名socket、监听socket
- 创建用于统一事件源的管道
- 创建内核事件表并注册事件
- 进入主循环，阻塞等待`epoll_wait`返回，遍历返回的就绪事件
  - I/O事件 - 监听socket可读（有新到达的客户连接）、连接socket可读/写（需要与客户进行I/O操作）、文件描述符上发生错误
  - 信号 - 统一事件源管道读端可读（有信号到达）
  - 定时事件 - `SIGALRM`信号到达
- 释放资源，退出程序