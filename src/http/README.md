# HttpConn类

HttpConn类描述了服务器与客户之间的连接，封装了一次请求与响应过程中所涉及的各种描述信息。其中比较重要的是读缓冲区和写缓冲区，读缓冲区用于存放请求报文的内容，写缓冲区用于存放响应报文的内容。它同时也是一个任务类，工作线程可以拿到某个该类的对象，然后调用其`Process`函数处理它，具体的处理操作主要涉及两个方面的工作，一是解析读缓冲区（即请求报文）中的内容；二是填充写缓冲区（即响应报文）中的内容。由于注册了`EPOLLONESHOT`事件以保证一个连接socket在任意时刻只被一个线程处理，因此每个线程处理完毕后，需要重新注册`epoll`事件。